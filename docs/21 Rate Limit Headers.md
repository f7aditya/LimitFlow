# Rate Limit Response Headers

## X-RateLimit-Limit

Maximum requests allowed during one window.

## X-RateLimit-Remaining

Remaining requests available before the limit is exceeded.

## Retry-After

Returned when the client is rate limited.

Specifies how many seconds remain before another request can be made.
